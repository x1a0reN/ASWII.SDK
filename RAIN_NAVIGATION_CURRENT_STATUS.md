# SurvivalBot 分支：RAIN 寻路现状与问题总结

更新日期：2026-07-22  
适用范围：仅 `Forks/SurvivalBot` 工作树及 `codex/survival-ai` 分支

## 1. 当前结论

当前的主要矛盾不是“RAIN 寻路效果差”，而是 **RAIN 运行时对象跨 Unity 场景的生命周期不安全**。

- 对 `level33` 而言，RAIN 生成的全图拓扑明显优于现有 `phys_grid_2_5d`，尤其是在多楼层、楼梯、斜坡、墙角和 Off-Mesh Jump/Drop Link 场景中。
- 现有崩溃不能再简单归因于缓存精度过高或内存不足。最近一次第二次进图崩溃前，x86 地址空间仍有较大的连续空闲区，但游戏仍在原生场景加载阶段发生了 `0xc0000005`。
- 当前部署版本已经改成“离场时注销 RAIN 图并销毁场景挂载点，下一场重新挂载和注册同一个托管图对象”，但这个方案 **尚未经过运行验证，不能宣称已经解决崩溃**。
- 如果目标是连续运行数天，最终应把“高精度导航数据”与“RAIN/Unity 场景对象”完全解耦。最稳妥的长期架构是导出自有紧凑导航格式，并由本项目自行执行全局寻路。

## 2. 当前分支快照

| 项目 | 当前状态 |
|---|---|
| 工作目录 | `D:\逆向\逆向-源码\创想兵团腾讯\ASWDEBUG\Forks\SurvivalBot` |
| Git 分支 | `codex/survival-ai`，跟踪 `origin/codex/survival-ai` |
| 当前 HEAD | `28c31c446d190f3c950ffa579e87f2c4247f0dfa` |
| HEAD 说明 | `feat: bake RAIN off-mesh navigation data` |
| 工作树 | 存在大量未提交修改；当前部署 DLL 不等同于上述 HEAD 的纯净产物 |
| 已部署 DLL | `C:\Users\x1a0reN\AppData\Local\ASWII\Data\ASWII_Data\Managed\ASWDEBUG.dll` |
| 已部署 DLL SHA-256 | `91D660703F2BEA9AD498981AB5B9E28E75F59FFE52603352FD881CDAFB892995` |
| 已部署 DLL 大小 | `421888` 字节 |

本文件只描述当前分支，不代表主工作树、其他分支或历史版本的状态。

## 3. 当前目标

限定只玩 `level33`，实现以下运行模型：

1. 使用 RAIN 级别的高精度全局寻路能力。
2. 地图缓存只生成一次，不在每局重复建图。
3. 玩家可反复进入和退出同一地图，不因第二次或后续进图崩溃。
4. 能连续执行本地巡回寻路测试和生存实战循环，目标运行时间为数天。
5. `phys_grid_2_5d` 不再承担全局规划，只允许作为局部碰撞确认、脱困或受控降级工具。

## 4. 已经具备的能力

### 4.1 导航数据

当前 `level33` 已存在以下缓存：

| 文件 | 大小 | 最后修改时间 |
|---|---:|---|
| `level33.rainnav` | 36,726,321 字节 | 2026-07-20 14:44:03 |
| `level33.rainmeta` | 9,176,089 字节 | 2026-07-20 17:42:19 |
| `level33.runtime.rainnav` | 20,997,212 字节 | 2026-07-21 21:35:11 |
| `level33.runtime.rainmeta` | 6,303,975 字节 | 2026-07-21 21:35:51 |

缓存目录：

```text
C:\Users\x1a0reN\AppData\LocalLow\____________II\ASWDEBUG\NavMeshCache
```

运行时版本当前使用 `0.20m` 单元精度。它相较此前最大精度版本降低了节点规模和内存压力，但仍保留了 RAIN 全局拓扑的主要优势。

### 4.2 已接入的导航信息

- 多楼层和连通分区。
- 墙体与可行走区域边界。
- 楼梯和斜坡通行。
- Jump/Drop 派生连接。
- 路径绘制、Bot 巡回测试和实战路径调用入口。
- 磁盘缓存命中和运行时缓存加载。

### 4.3 当前代码中的场景切换处理

当前未验证补丁的意图是：

1. 离开场景时从 RAIN 中注销导航图。
2. 清除 `MountPoint`，销毁旧场景的挂载宿主。
3. 保留已经物化的托管导航图，不再次从磁盘反序列化或重建。
4. 下一次 `Level.Ready` 后创建新的场景宿主、重新设置挂载点并注册原图。
5. 不再对宿主使用 `DontDestroyOnLoad`。

相关状态日志设计包括：

```text
register=removed mount=destroyed
register=pending remount=pending
register=1 remount=1
direct_load_preflight_passed
```

该方案已经构建并部署，但还没有经过下一轮实际进出图验证。

## 5. 最近一次崩溃的决定性证据

日志：

```text
C:\Users\x1a0reN\AppData\LocalLow\____________II\Logs\ASW_SurvivalBot.pid2104.log
```

### 5.1 第一次进入

运行时缓存成功载入：

```text
runtime_disk_loaded ... nodes=364991
resident_pinned ... private=1661898752 addressFree=2047148032 largestFree=1888612352
```

随后本地 RAIN 巡回寻路能够开始工作。

### 5.2 退出第一局

旧方案在退出时仍保留注册状态：

```text
resident_suspended ... register=kept
```

当时大致状态：

- 托管内存约 `834 MB`。
- Private Bytes 约 `1.778 GB`。
- 空闲地址空间约 `1.937 GB`。
- 最大连续空闲区约 `1.838 GB`。

### 5.3 第二次进入

日志最后到达：

```text
direct_load_requested
direct_load_started
```

没有到达：

```text
resident_resume_armed
```

约一秒后 Windows 记录：

```text
Process: ASWII.exe
Exception: 0xc0000005
Fault module: unknown
Fault offset: 0x05b40c41
```

这把故障边界收窄到了：**游戏原生场景加载已经开始，但本项目还没有进入驻留图恢复流程时就崩溃了。**

## 6. 已排除或尚未证明的判断

### 6.1 不能再把最近崩溃归因于地址空间不足

第二次加载前仍有：

```text
addressFree=1947660288
largestFree=1838084096
```

因此，“内存门槛未拦住低内存加载”不能解释这一次崩溃。

此前约 `625403` 节点的最大精度方案确实造成过明显内存压力；降低到约 `364991` 节点有价值，但它只降低风险，不能修复跨场景悬挂引用、重复注册或原生对象失效。

### 6.2 尚未得到原生调用栈

目前只有异常码、故障偏移和发生阶段，没有可用的原生崩溃调用栈。因此以下判断仍是高概率推断，而不是已证明的唯一根因：

- RAIN 注册表保留了已经属于旧场景的对象或组件。
- `DontDestroyOnLoad` 宿主与新场景的 RAIN 管理器发生冲突。
- 托管导航图内部仍引用旧 `NavigationManager`、Unity 对象、场景 Collider 或其他原生句柄。
- 场景卸载和 RAIN 后台任务存在竞态。

### 6.3 日志不是这次原生崩溃的主要证据方向

大量逐帧日志会造成卡顿和磁盘开销，应继续限流；但最近一次崩溃发生在明确的原生场景加载边界，现有证据不足以把根因归为“写日志太多”。

## 7. 根本问题：数据质量与运行时生命周期被混在了一起

RAIN 缓存实际包含两类价值完全不同的东西：

1. **可持久化导航数据**：多边形、邻接关系、分区、边界、楼层、Jump/Drop Link 等。
2. **场景绑定运行时对象**：RAIN 管理器、注册关系、Unity `GameObject`、`Transform`、Collider、后台查询状态等。

第一类适合跨局复用；第二类通常只在创建它的场景中有效。当前反复崩溃的核心风险，就是为了复用第一类数据而同时保留了第二类对象或引用。

因此：

- “只从磁盘读一次”不等于“把整个 RAIN 运行时图永久注册一次”。
- “缓存已经生成”不等于“运行时图可安全跨场景存活”。
- “降低精度”能减少内存，但不能使失效引用重新有效。

## 8. RAIN 与 `phys_grid_2_5d` 的实际关系

### 8.1 RAIN 当前更适合全局规划

对于 `level33`，RAIN 的优势包括：

- 能表达真实可行走面和墙体边界。
- 能区分上下楼层，减少同一 X/Z 坐标导致的垂直误连。
- 能表达楼梯、斜坡和复杂拐角。
- 能预生成 Jump/Drop Link。
- 可以基于全局拓扑生成完整路径，而不是仅依赖玩家附近的即时物理采样。

### 8.2 当前 2.5D 的主要缺陷

- 多楼层投影混淆。
- 墙角、楼梯和狭窄通道容易来回切点。
- 局部采样导致 `no path`、`pending` 或错误降级。
- 容易在正确 RAIN 路径和错误 2.5D 路径之间反复切换。

结论是：**不应因为 RAIN 生命周期实现有问题，就退回 2.5D 作为全局寻路器。** 正确方向是保留 RAIN 数据质量，同时去掉对跨场景 RAIN 运行时对象的依赖。

## 9. 可选架构方案

| 方案 | 做法 | 优点 | 风险/代价 | 当前评价 |
|---|---|---|---|---|
| A. 每场重新物化 | 复用磁盘缓存；每次 `Level.Ready` 后创建本场 RAIN 图；离场前停止查询、注销并释放全部本场对象 | 生命周期最清晰；最容易先达到稳定 | 每局约有一次反序列化/初始化开销；需验证释放完整 | 短期稳定优先方案 |
| B. 自有紧凑导航运行时 | 从 RAIN 缓存导出完整拓扑到 `level33.aswnav`；运行时只加载紧凑数组，由本项目执行 A*、Funnel 和局部跟随 | 真正一次加载；不依赖 RAIN 场景生命周期；适合连续运行数天 | 需要实现导出格式、规划器和验证工具 | 长期推荐方案 |
| C. 保留托管图并重新注册 | 离场注销和销毁挂载点，但保留物化后的 RAIN 图；下一场重新挂载注册 | 避免重复磁盘读和重建；改动相对小 | 图内部可能仍含旧场景/旧管理器引用；目前未验证 | 当前已部署的实验方案，不是最终结论 |

## 10. 建议实施路线

### 阶段一：先建立可信的稳定基线

1. 暂停继续叠加“强制 GC、延迟、内存阈值、场景回滚”等补丁式处理。
2. 采用方案 A：缓存不重建，但 RAIN 运行时图严格按场景创建和销毁。
3. 在退出前按固定顺序执行：停止寻路请求 → 等待 Worker 停止 → 注销导航图 → 清除所有持有者引用 → 允许场景卸载。
4. 进入新场景后等待 `Level.Ready`，再物化并注册当前场景专用图。
5. 不在 Unity 场景切换的关键窗口强制执行 `GC.Collect` 或 `UnloadUnusedAssets`。
6. 连续执行至少 20 次本地测试进出图，确认没有崩溃和单调内存增长。

现有日志表明，`0.20m` 运行时缓存从磁盘命中到注册大约需要十余秒，理论上能利用每局约 20 秒的赛前等待时间。但必须以实际连续测试结果为准。

### 阶段二：构建真正可长期驻留的导航内核

基于现有 RAIN 生成结果，导出完整、无 Unity 对象引用的导航文件：

```text
level33.aswnav
```

至少包含：

- 多边形顶点和索引。
- 多边形邻接关系。
- Portal/走廊边。
- 连通分区和楼层信息。
- 可行走边界及与障碍物的安全距离。
- Off-Mesh Jump/Drop Link 及方向、起点、终点和代价。
- 空间索引，用于将玩家和目标坐标投影到最近有效多边形。
- 出生点、避难点、悬崖候选和其他战术标记。

运行时只保留紧凑值类型数组，使用自有 A* + Funnel 生成全局路径；物理检测只负责局部可行性、跳跃触发和脱困。这样才能真正实现“一次加载，反复进局，不携带旧场景对象”。

## 11. 验收标准

### 11.1 稳定性

- 本地巡回寻路测试连续进出 `level33` 至少 20 次不崩溃。
- 生存模式连续至少 50 局不崩溃。
- 进行长时间挂测，Private Bytes、托管堆和最大连续空闲地址区不存在持续恶化趋势。
- 每局最多存在一个有效导航图注册实例，不出现重复 Worker、重复宿主或旧场景挂载点。

### 11.2 生命周期可观测性

每次场景切换必须能在日志中串起唯一序列：

```text
stop_requests
workers_idle
graph_unregistered
scene_released
level_ready
graph_materialized_or_bound
graph_registered
navigation_ready
```

序列应带场景代次 ID 和图实例 ID，以便证明第二局没有复用第一局的场景对象。

### 11.3 寻路质量

- 全局路径默认只能由 RAIN 拓扑或后续自有 `aswnav` 拓扑生成。
- 不出现玩家在正确路径和 `phys_grid_2_5d` 错误路径之间来回切换。
- 楼梯和普通斜坡默认直接行走，只有确认位置长期无推进后才尝试跳跃。
- 拐角路径保留角色半径和转弯余量，禁止把路径点放到墙内、栏杆内或上下层错误投影点。
- Jump/Drop 仅由已验证的 Off-Mesh Link 或实时安全确认触发。

## 12. 当前决策点

当前应明确区分“实验补丁”和“可交付架构”：

- 已部署的方案 C 只值得做一次受控验证，用来判断“重新注册同一托管图”是否仍会触发旧引用问题。
- 如果仍在第二次进图或退出时崩溃，不再继续围绕该方案叠加清理补丁，直接切换到方案 A 建立稳定基线。
- 达到稳定基线后推进方案 B，才符合“只玩一张图、只加载一次、连续挂数天”的最终目标。

在完成连续进出图验收前，不能写出“已经保证无论进入多少次都不会崩溃”的结论。

## 13. 关键代码与证据位置

代码范围：

```text
D:\逆向\逆向-源码\创想兵团腾讯\ASWDEBUG\Forks\SurvivalBot
```

重点模块：

```text
RuntimeRainNavMesh.cs
MapBakeSceneLoader.cs
SurvivalBotRoutePlanner.cs
```

运行日志：

```text
C:\Users\x1a0reN\AppData\LocalLow\____________II\Logs
```

导航缓存：

```text
C:\Users\x1a0reN\AppData\LocalLow\____________II\ASWDEBUG\NavMeshCache
```

部署目标：

```text
C:\Users\x1a0reN\AppData\Local\ASWII\Data\ASWII_Data\Managed\ASWDEBUG.dll
```

## 14. 本文档未覆盖的范围

- 主工作区或其他 Git 分支的实现状态。
- 自动匹配、翻牌、GM 观战处理、网络转发、ESP 和职业攻击策略的完整功能清单。
- 尚未由当前分支运行日志重新验证的历史问题。
- 对 `level33` 以外地图的稳定性承诺。

本文档聚焦当前阻塞最大的 `level33` RAIN 缓存加载、场景生命周期、重复进出图崩溃和全局寻路架构问题。
